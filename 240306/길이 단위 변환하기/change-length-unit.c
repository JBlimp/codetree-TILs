#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    double oneft = 30.48;
    double onemi = 160934;
    double a = 9.2;
    double b = 1.3;
    printf("%.1lfft = %.1lfcm", a, a*oneft);
    printf("%.1lfmi = %.1lfcm", b, b*onemi);
    return 0;
}